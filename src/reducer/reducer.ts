/* import { InvoiceListType } from '../interfaces/globalContextInt';
import { ActionObjectType } from '../interfaces/reducerTypes';

export default function InvoiceReducer(state: InvoiceListType, action: ActionObjectType) {

    switch (action.type) {

    }
} */